{
  hypnotoad => {
    listen => ['http://*:8025'],
    workers => 2,
    pid_file => 'badland.pid'
  },
  # Tile background colours — add, remove, or change freely.
  # They cycle in order; the count determines the cycle length.
  tile_colors => [
    # '#1a237e',
    # '#4a148c',
    # '#880e4f',
    # '#bf360c',
    # '#1b5e20',
    # '#006064'
    '#000000',
    #'#ffffff',
    #'#939393'
  ],

  # Speech bubble phrases for the breakdancer. Randomly selected while playing.
  # Keep at least one entry uncommented. Defaults to ['fresh'] if empty.
  dancer_phrases => [
    'fresh',
    'booyah',
    'spin it',
    'old school',
    'dope',
    '🔥',
  ],
}

